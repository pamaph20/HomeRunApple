Run API file with  python -m uvicorn api:app --host 127.0.0.1 --port 8000
Run Data File with  python -m uvicorn data:app --host 127.0.0.1 --port 8010

will have to hostLive mlb api on some web server soemthing.
